@namespace BlazorClient
@using BlazorClient.Components

<Router AppAssembly="@typeof(App).Assembly">
    <Found Context="routeData">
        <Frame HeaderContent="@Header" FooterContent="@Footer" LoginOutFormContent="@LoginOutForm">
            <RouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)" />
        </Frame>
    </Found>
    <NotFound>
        <LayoutView Layout="@typeof(MainLayout)">
            <p>Sorry, there's nothing at this address.</p>
        </LayoutView>
    </NotFound>
</Router>

@code {
    private RenderFragment Header => @<HeaderComponent />;
    private RenderFragment Footer => @<FooterComponent />;
    private RenderFragment LoginOutForm => @<LoginOutForm />;
}
